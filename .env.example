# Database Configuration
# Path to the SQLite database file (relative to project root or absolute path)
DATABASE_PATH=data/cookbook.db

# GitHub token for auto-creating PRs from recipe submissions
# Needs contents:write and pull_requests:write on ni3do/cookbook-website
GITHUB_TOKEN=
